# 工作留言板（三重龍門 × 西門）

交接班用的留言板：同事可以留「客戶預訂單 / 任務交接 / 例行工作」，
預訂單按下「確認取貨完成」就會自動扣掉 Google Sheet 裡的庫存。

- 資料庫：Google Sheet「進銷存總表－dragon2gates_bot」
  （ID `1OZsbv9IWADz0FfBxQIaDuAXXhW5U40SRzOhjIRIPIww`）
- 留言存在同一本試算表的 **「留言板」** 工作表（第一次開啟時程式會自動幫你建立）
- 庫存讀寫的是 **「Products產品」** 工作表
- 登入方式：Google 帳號登入（和 pnl-dashboard 同一個 Google Cloud 專案）

> ⚠️ 這個專案和 `pnl-dashboard` **完全獨立**：不同資料夾、不同 repo、不同網址、不同試算表。
> 唯一共用的是 Google 登入的用戶端 ID（只需要多加一個「已授權來源」，不會改到原本的設定）。

---

## 檔案

| 檔案 | 用途 |
|---|---|
| `index.html` | 網頁畫面與樣式 |
| `app.js` | 全部程式邏輯（最上面的 `CONFIG` 區塊是設定值） |
| `同事操作說明.md` | 給店裡同事看的使用說明＋庫存計算邏輯 |
| `README.md` | 這份文件（給你自己看的） |

**不需要 npm、不需要 build、不需要部署指令。** 改完檔案上傳 GitHub 就生效。

---

## 第一次上線（大約 15 分鐘）

### 步驟 1　建立 GitHub repo

1. 打開 https://github.com/new
2. Repository name 填 **`shop-board`**
3. 選 **Public**（GitHub Pages 免費方案只支援公開 repo；資料本身還是靠 Google 權限保護，外人打不開你的試算表）
4. 按 **Create repository**

### 步驟 2　上傳檔案

1. 在新 repo 頁面按 **uploading an existing file**
2. 把桌面 `shop-board` 資料夾裡的 **`index.html`**、**`app.js`**（其他 .md 檔要不要上傳都可以）拖進去
3. 按 **Commit changes**

### 步驟 3　開啟 GitHub Pages

1. repo 上方 **Settings** → 左邊選單 **Pages**
2. Source 選 **Deploy from a branch**
3. Branch 選 **main**、資料夾選 **/ (root)** → **Save**
4. 等 1～2 分鐘，網址就是：
   **https://willinlee20.github.io/shop-board/**

### 步驟 4　讓 Google 登入認得這個新網址 ← 最容易漏掉的一步

1. 打開 https://console.cloud.google.com/apis/credentials
2. 上方專案切到 **TOOLA MASTER DASHBOARD**
3. 點「OAuth 2.0 用戶端 ID」裡那個 `936097912364-...` 的項目
4. 在 **已授權的 JavaScript 來源** 按「新增 URI」，填入：
   ```
   https://willinlee20.github.io
   ```
   （如果這一行已經存在，就不用加，直接跳過）
5. 按 **儲存**。Google 有時要等 5 分鐘才生效，若登入失敗請稍等再試。

> 這一步只是「多加一個允許的網址」，**不會影響 pnl-dashboard**。

### 步驟 5　開權限給同事

1. 打開「進銷存總表」→ 右上角 **共用**
2. 把同事的 Google 帳號加成 **編輯者**（要能寫入才能扣庫存）
3. 如果你的 Google Cloud OAuth 同意畫面還是「測試中」狀態，
   到 https://console.cloud.google.com/apis/credentials/consent
   把同事的 Gmail 加到 **測試使用者**，否則他們登入會被拒絕。

### 步驟 6　實測一次

用手機打開網址 → 登入 → 開一張金額很小的測試預訂單 →
去試算表看 `Products產品` 的「預定專區」有沒有 +N、門市庫存有沒有 -N →
回網頁按「取消預訂」→ 看數字有沒有還原 → 沒問題就可以給同事用了。

---

## 之後要改東西怎麼做

在 GitHub 上直接改：repo → 點 `app.js` → 右上角鉛筆圖示 → 改完按 **Commit changes** →
等 1 分鐘，網頁重新整理（手機建議用「重新載入」）就會看到新版。

`app.js` 最上面的 `CONFIG` 區塊可以直接改的東西：

| 設定 | 說明 |
|---|---|
| `BOARD_SHEET` | 留言要寫在哪個工作表，預設 `留言板` |
| `STORES` | 門市名稱與對應的庫存欄位。`label` 是畫面顯示的名字，`col` 必須跟 `Products產品` 的欄位標題**一模一樣** |
| `SLOTS` | 取貨時間範圍選項 |
| `SOURCES` | 客戶來源選項 |
| `ROUTINES` | 例行工作的四個選項（要加第五個直接加一行） |
| `POLL_SECONDS` | 每幾秒自動抓一次新留言，預設 45 秒 |

程式是用**欄位標題**去找欄位的（不是寫死 D 欄、E 欄），
所以在 `Products產品` 中間插入新欄位不會壞掉；但**不要改欄位標題的文字**。

---

## 有一件事請你確認

`Products產品` 的 **「總數」** 欄（目前在 J 欄）：

- 如果它是**公式**（例如 `=D2+E2+F2+G2`）→ 最理想，程式不會去動它，它會自己跟著更新。
- 如果它是**手打的數字** → 程式會在扣庫存時，幫它重算成
  `三重店 + 西門店 + 預定專區 + 總倉數量`。

我看資料時發現有一列（酪梨倉 0.7）的總數 `9` 和各欄相加 `3+3+6=12` 對不起來，
所以這欄可能有手動改過的痕跡。**建議把整欄統一改成公式 `=D2+E2+F2+G2` 往下拉**，
這樣庫存數字最不容易出錯。如果你的「總數」公式定義跟這個不一樣，跟我說我再調程式。

---

## 疑難排解

| 症狀 | 原因 / 處理 |
|---|---|
| 按登入沒反應、或跳出 `redirect_uri_mismatch` / `origin` 錯誤 | 步驟 4 沒做，或還沒生效（等 5 分鐘） |
| 登入後顯示「沒有權限存取這份試算表」 | 這個 Google 帳號還沒被加入試算表的編輯者（步驟 5） |
| 顯示「找不到產品庫存工作表」 | `Products產品` 工作表被改名了，改 `CONFIG.PRODUCT_SHEET_KEYWORDS` |
| 顯示「缺少欄位：…」 | `Products產品` 的欄位標題被改過，改回來或改 `CONFIG.H` / `CONFIG.STORES` |
| 卡片上出現「重試扣庫存」 | 送出時網路斷掉，庫存還沒扣。按那顆按鈕即可（不會重複扣） |
| 同事看不到別人剛留的言 | 每 45 秒會自動更新，也可以按右上角「↻ 更新」 |
| 庫存數字怪怪的 | 試算表有版本紀錄（檔案 → 版本記錄），可以查是哪一筆動的；每張單的扣法也記在「留言板」工作表的 `庫存異動JSON` 欄 |

---

## 留言板工作表的欄位

| 欄 | 標題 | 說明 |
|---|---|---|
| A | id | 系統編號 |
| B | 類型 | 客戶預訂單 / 備貨 / 報廢 / 任務交接 / 例行工作 |
| C | 建立時間 | |
| D | 建立者 | 登入的 Google 帳號名稱 |
| E | 門市 | 三重龍門 / 西門 |
| F | 狀態 | 待處理 / 已完成 / 已取消 |
| G | 客戶名稱 | |
| H | 客戶來源 | 店內 / 網路 / 親友（**報廢單**借這一欄存「報廢原因」） |
| I | 取貨日期 | 備貨單＝備貨日期；報廢單＝報廢日期 |
| J | 取貨時段 | |
| K | 品項明細 | 人看的文字版 |
| L | 金額 | |
| M | 備註 | 任務交接的內容也放這裡 |
| N | 例行工作項目 | |
| O | 完成時間 | |
| P | 完成者 | |
| Q | 品項JSON | 程式用，請勿手改 |
| R | 庫存異動JSON | 程式用，記錄這張單怎麼扣的，取消時靠它還原 |
| S | 庫存狀態 | 已預留 / 已出庫 / 已轉入門市 / 已還原 / **已報廢扣除** / **報廢已復原** / 未扣 / 不適用 |
| T | 最後修改時間 | |
| U | 最後修改者 | |
| V | 修改紀錄 | |
| W | 關聯單號 | 「送貨到門市」跟單所對應的預訂單 id；有值代表這張單不動庫存 |
| X | 折扣 | 預訂單的折扣金額（直接減多少錢），沒折扣是 0 |
| Y | 未折金額 | 折扣前的品項合計（L 欄「金額」存的是折扣後） |
| Z | 收款方式 | 現金 / 匯款；空白視同現金。取貨結案時原封帶到來店銷售單 |

這張表可以直接拿來做報表（例如用樞紐分析看每月預訂金額、哪個來源的客人最多）。

---

## 版本紀錄

網頁左上角「西門龍門儀錶板」旁邊會顯示目前版本（例如 `v2.0`）。
每次我改版都會加號碼，你上傳完看那裡就知道有沒有更新成功。

| 版本 | 日期 | 內容 |
|---|---|---|
| **v3.7** | **2026-09-23** | 網路單：① 每個品項可標 **保固換貨／點數換贈／公關贈品**——不算營收、不算成本，**庫存照扣**，結帳狀態不自動改 ② 庫存處理新增 **從三重龍門／西門出貨**（扣在選的那間店，品項的庫存數字也跟著看那裡）③ 網路應收待結上方新增 **待會計確認小計**（已結帳、貨態都完成的金額加總），每個階段鈕也帶小計 ④ 卡片上顯示並可改 **已收貨款／貨到付款**；改成「已收貨款」時結帳狀態自動跟上，已收貨款的單不准改回未結帳 ⑤ **修正：改單時貨到付款會把已收到的錢壓回「未結帳」**（結帳日、結帳確認者也會被清掉）⑥ 「未結帳」階段改成只看**已寄出**的單，還沒寄的不算欠款 ⑦ 小賣／經銷寄送方式新增 **收件者提供寄件代號**，選了只要填代號，不用店名／收貨門市／地址 |
| **v3.6** | **2026-09-20** | 預訂單三項調整：① 新增 **折扣**（跟來店／網路單同一套：金額存折扣後，另存未折金額）② 新增 **收款方式（現金／匯款，預設現金）**，取貨結案時原封帶到來店銷售單 ③ **修正：按「確認取貨完成」會把業務變成按的那個人**——現在自動產生的來店銷售單，負責業務一律記**當初開單的人**，按確認的人另外記在建立者與備註（他只是幫忙出貨）。確認視窗會先把應收金額、收款方式、業務是誰列出來。留言板分頁自動補 折扣／未折金額／收款方式 三欄；舊單沒有這幾欄 → 折扣 0、收款現金、業務用建立者 |
| **v3.5** | **2026-09-19** | ① 來店／網路單新增 **折扣**（直接減多少錢，扣完才是銷貨金額；`金額`/`價格` 存折扣後，另存 `未折金額`，程式自動補欄）② 網路應收待結新增 **四段篩選**（未處理／未取件／未結帳／待會計確認）＋每張卡的四關進度條 ③ 網路單新增最後一關 **✅ 會計確認（結案）**：寄件、取件、結帳都完成後才出現，按了才從清單消失（`Online網路` 自動補 會計確認／會計確認者 欄）④ 留言板「＋新增留言」改成 **＋新增作業**、「留言類型」改成 **作業類型** ⑤ 新增 **報廢** 作業類型：選來源、填數量與原因，送出直接負向扣庫存（總數會減少、不進預定專區），打錯可按「↩ 復原報廢」原路加回 |
| **v3.4** | **2026-09-19** | 全流程稽核（33 項檢查全過，現行扣庫存邏輯無誤）。新增一道防護：備貨單標著「已預留」但沒有記錄當初扣了哪些貨（庫存異動JSON 空的）時，按「備貨完成」會跳紅字警告說明庫存不會有任何變動，不再默默完成 |
| **v3.3** | **2026-09-18** | 新增網路銷售單的結帳狀態**預設改成「貨到付款」**（原本是已收貨款）。改單時照樣帶回那張單原本的收款方式，不受新預設影響 |
| **v3.2** | **2026-09-18** | 「今日銷售」改成跟著**營業日**走：來店單看該門市的營業日（日結後就是隔天），結完帳繼續賣的單不會從首頁消失；同時列到兩天的資料時會把日期標出來。銷售首頁也加上「今日營業」門市卡（可直接日結）。「品項健檢」改名為 **資料庫品項健檢** |
| **v3.1** | **2026-09-18** | 來店單新增 **收款方式（現金／匯款，預設現金）**，紀錄卡的金額旁邊會標，近 7 天業績也拆現金／匯款（`ShopSales店頭銷售` 需要 **收款方式** 欄，程式自動補）；來店銷售紀錄新增 **🔒 本日營業結束**（每間門市各自結）：當天簡易報表、日期分隔線、結束後來店單預設日期跳隔天，可 **↩ 取消日結**（新增 `DayClose日結` 分頁，程式自動建） |
| **v3.0** | **2026-09-18** | 新增 **🩺 品項健檢**（銷售紀錄首頁）：一次把還沒結案的單全部拿去跟現在的庫存表對，列出「找不到品項」的單（現在不能改單）和「名稱只差空白／大小寫」的單，並統計要在庫存表補回來的名稱 |
| **v2.9** | **2026-09-18** | **修正嚴重 bug：改單（例如只改日期）會把品項換成別的產品。** 存檔時品項的身分改用「產品名稱＋規格」判定，不再用列號（庫存表插／刪列之後列號會指到別的產品）；名稱只差空白或大小寫會自動對回來；真的找不到就跳訊息擋住整張單，不再默默猜一個 |
| **v2.8** | **2026-09-18** | 所有「數量」欄位加上 **−／＋ 按鈕**（按一下加減 1，還是可以直接打字）：留言板的預訂／備貨、四種銷售單、經銷的規格表都有；規格表的欄位順便對齊，手機上規格名稱不再一個字一行 |
| **v2.7** | **2026-09-18** | 修正：已收起（封存）的單還會出現在「今日銷售」；小賣／經銷的作廢單補上「✓ 收起」，小賣自取（寄件取貨都是 N/A）的已結帳單也能收起；作廢的單在應收待結會劃掉並標「已作廢」 |
| **v2.6** | **2026-09-18** | 來店銷售紀錄：上面加「近 7 天業績」（筆數／金額／各門市／各業務／毛利，作廢的不算）；作廢的單多一顆「✓ 收起」，收起後不再顯示但試算表資料完整保留（`ShopSales店頭銷售` 需要 **封存** 欄，程式會自己補） |
| **v2.5** | **2026-09-18** | **修正嚴重庫存 bug**：改單時改用「產品名稱＋規格」找回品項（原本只信列號，庫存表插過列之後會抓到完全不同的產品，導致品項和數量都跑掉）；品項在庫存表找不到時擋下修改，不再默默丟掉 |
| v2.4 | 2026-09-17 | 「退貨入庫／訂單作廢」改依寄件狀態自動切換（已寄出→退貨入庫、未寄出→作廢）；狀態按鈕點一下直接存 |
| v2.3 | 2026-09-17 | 四種銷售單都可以「✎ 改單」（改品項／數量／價格／客戶資料），庫存改用差額計算，沒改到的品項不會被碰 |
| v2.2 | 2026-09-17 | 網路應收待結改成撿貨清單：一張單一張卡、品項放大、寄件／取貨／結帳狀態改成按鈕；四種單別都加「訂單作廢」（庫存原路退回）；未寄出的排最前面 |
| v2.1 | 2026-09-17 | 網路單新增「庫存處理」三選一：扣總倉（預設）／廠商代出／待調貨後出貨，後兩者不扣庫存但照記營收成本；Online網路 新增「庫存狀態」欄 |
| **v2.0** | **2026-09-16** | **改版成「西門龍門儀錶板」：新增銷售紀錄模組**（來店／網路／小賣／經銷四種單別、今日銷售逐筆、兩層應收待結、來店銷售紀錄可改可作廢、退貨入庫）；頂端加「銷售紀錄／工作留言板」切換；招財貓圖示 |
| v1.9 | 2026-09-16 | 圖示改成 SVG；輸入框寬高統一（電話欄原本沒吃到樣式） |
| v1.8 | 2026-09-16 | **修正庫存重複扣的 bug**：改單時改用「只算差額」，沒改到的品項不會被碰到 |
| v1.7 | 2026-09-16 | （同 v1.8，中途版本） |
| v1.6 | 2026-09-14 | 預訂單品項選到「總倉」時跳黃底提示，並可自動開一張「送貨到門市」的跟單備貨單（不動庫存）；留言板新增 W 欄「關聯單號」 |
| v1.5 | 2026-09-13 | 備貨改成表格式輸入（一支產品的所有規格一次填數量）；加上版本號與版本不一致警示 |
| v1.4 | 2026-09-13 | 品項加入分類分頁（讀庫存表的「分類」欄）、記住上次選的分類與來源；讀取範圍放大到 3000 列 |
| v1.3 | 2026-09-13 | 新增「備貨」類型；每個品項可指定出貨來源（預設總倉）；取消時可選退回位置 |
| v1.2 | 2026-09-12 | 修改留言＋修改紀錄（誰改了什麼）；已取消的紀錄可刪除（兩道確認） |
| v1.1 | 2026-09-12 | 品項改成兩層下拉（產品名稱 → 產品規格） |
| v1.0 | 2026-09-12 | 初版：預訂單／任務交接／例行工作、預定專區庫存邏輯 |

### 改版時我會做的事

1. `app.js` 最上面的 `APP_VERSION` 加號碼
2. `index.html` 的 `<meta name="app-version">` 和 `<script src="app.js?v=…">` 一起改成同一個號碼

`?v=` 那一段是**防止瀏覽器拿舊檔**用的。號碼一變，瀏覽器就一定會重新下載 `app.js`，
所以之後**通常不用再按 Ctrl+Shift+R**。

如果兩個檔案的版本對不起來（例如只上傳了 app.js），開啟網頁時版本號會變紅色 ⚠️
並跳出提醒，告訴你哪個檔案是舊的。
